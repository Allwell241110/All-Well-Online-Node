<main>

  <!-- Filters (search, category, sort) -->
  <%- include('../partials/products/productFilters', {
    sort,
    searchQuery,
    currentPage,
    limit
  }) %>

  <!-- Product listing -->
  <%- include('../partials/products/productList', { products }) %>

  <!-- Pagination controls -->
  <%- include('../partials/products/pagination', {
    currentPage,
    totalPages,
    sort,
    limit
  }) %>

</main>